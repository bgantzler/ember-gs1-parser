import applicationIdentifiers from 'ember-gs1-parser/utils/application-identifiers';
import { module, test } from 'qunit';

module('Unit | Utility | application-identifiers', function () {
  test('it works', function (assert) {
    // let result = applicationIdentifiers;
    assert.ok(applicationIdentifiers);
  });
});
